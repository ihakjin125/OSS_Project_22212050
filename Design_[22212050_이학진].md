# 2. Design
<img width="2816" height="1536" alt="왓투웨어로고" src="https://github.com/user-attachments/assets/0c3c9f1f-136f-4092-a8b4-4b55ec83d665" />

**Title:**  WhatToWear(왓 투 웨어)

**Student No:** 22212050

**Name:** 이학진

**E-mail:** ihakjin125@gmail.com

---
## [ Revision history ]
| Revision date | Version | Description | Author |
| :--- | :--- | :--- | :---|
| 2026-05-08 | 1.0.0 | First Document | 이학진 |

---

##  Contents 
1. [Introduction](#1-introduction)
2. [Class_diagram](#2-class-diagram)
3. [Sequence_diagram](#3-sequence-diagram)
4. [State_machine_diagram](#4-state-machine-diagram)
5. [Implementation_requirements](#5-implementation-requirements)
6. [Glossary](#6-glossary)
7. [References](#7-references)

## 1. Introduction
이 문서는 기상 데이터와 사용자 맞춤형 알고리즘을 융합한 날씨 기반 옷차림 추천 서비스, **'WhatToWear'**의 시스템 설계(Design)단계를 요약한 문서입니다. 
본 문서에는 시스템의 전반적인 아키텍처를 정의하는 클래스 다이어그램, 모듈 간의 데이터 흐름을 보여주는 시퀀스 다이어그램, 클라이언트와 서버의 상태 변화를 나타내는 상태 머신 다이어그램 및 상세한 구현 요구사항이 포함되어 있습니다.
### 핵심 설계 포인트
본 시스템 설계의 가장 중요한 핵심 포인트는 다음과 같습니다.
- 엄격한 MVC 아키텍처 적용 : Spring Boot 프레임워크를 활용하여 MVC(Model-View-Cntroller)패턴을 적용함으로써
  프론트엔드(UI)와 백엔드(비즈니스 로직)의 책임을 명확히 분리하고 시스템의 유지보수성을 높였습니다.
- 동적 추천 알고리즘 설계: 기상청 API의 단순 기온을 넘어, 사용자별 '체질 가중치'를 반영해 개인화된 체감 온도를 산출합니다.
- 유연한 예외 처리 및 피드백 구조: 사용자 옷장에 조건과 완벽히 일치하는 옷이 없을 경우 시스템 오류를 방지하고 대체 카테고리 의상을 제안하는
  유연한 예외 처리 로직을 구현했습니다. 또한, 사용자의 피드백을 실시간으로 DB에 반영하여 알고리즘을 고도화하는 학습구조를 구축했습니다.

## 2. Class diagram
### [Backend Classes]
<img width="1703" height="961" alt="image" src="https://github.com/user-attachments/assets/26d37f94-70de-4f97-b398-daaab3626215" />



### 1) MainController

* **Attributes**
    * `- memberService: MemberService` : 회원 데이터 및 가중치 업데이트를 처리하는 서비스 객체
    * `- recommendationService: RecommendationService` : 옷차림 추천 알고리즘을 처리하는 서비스 객체
* **Methods**
    * `+ getRecommendation(memberId: Long, tpo: String): OutfitGuide` : 사용자 ID와 외출 목적(TPO)을 입력받아 맞춤형 옷차림 가이드 결과를 반환
    * `+ submitFeedback(memberId: Long, request: FeedbackRequest): void` : 추천받은 옷차림에 대한 사용자의 피드백 점수를 수신하여 가중치 업데이트 요청
* **Description**
    * `MainController` 클래스는 Controller의 역할을 한다. 홈 화면에서 발생하는 날씨 기반 옷차림 추천 요청과 사용자의 체감 온도 피드백 제출 기능의 핵심 흐름을 제어하는 클래스이다.

### 2) ClothesController

* **Attributes**
    * `- clothesRepository: ClothesRepository` : 옷장 DB 접근을 위한 리포지토리 객체
    * `- memberService: MemberService` : 옷을 등록할 회원 정보를 조회하기 위한 서비스 객체
* **Methods**
    * `+ addClothes(memberId: Long, clothes: Clothes): void` : 특정 사용자의 디지털 옷장에 새로운 옷(두께, TPO 등)을 등록
    * `+ getClothesList(memberId: Long): List<Clothes>` : 특정 사용자가 등록한 모든 옷의 목록을 가져오기
    * `+ deleteClothes(clothesId: Long): void` : 옷장에 등록된 특정 옷 데이터를 삭제
* **Description**
    * `ClothesController` 클래스는 Controller의 역할을 한다. 디지털 옷장(Wardrobe) 화면에서 발생하는 의류의 등록, 목록 조회, 삭제(CRUD) API 요청을 전담하여 처리하는 클래스이다.

### 3) RecommendationService

* **Attributes**
    * `- weatherService: WeatherService` : 기상청 실시간 날씨 데이터를 불러오는 서비스 객체
    * `- clothesRepository: ClothesRepository` : 필터링할 사용자의 옷 데이터를 가져오는 리포지토리 객체
* **Methods**
    * `+ calculateTargetThickness(temp: Double, weight: Double): String` : 실시간 기온과 사용자의 체질 가중치를 연산하여 현재 날씨에 가장 적합한 옷의 두께(Thickness)를 산출
* **Description**
    * `RecommendationService` 클래스는 Service의 역할을 한다. 맞춤형 체감 온도를 산출하고 적합한 옷을 필터링하며, 일치하는 옷이 없을 경우 대안(Plan B)을 제시하는 본 시스템의 가장 핵심적인 알고리즘 비즈니스 로직을 수행하는 클래스이다.

### 4) MemberService

* **Attributes**
    * `- memberRepository: MemberRepository` : 회원 DB에 접근하기 위한 리포지토리 객체
* **Methods**
    * `+ findByLoginId(loginId: String): Member` : 로그인 아이디를 기반으로 회원 정보를 조회
    * `+ updateWeight(memberId: Long, score: Double): void` : 사용자의 피드백 점수를 받아 보정 상수를 적용한 후 기존 체질 가중치를 재계산하여 갱신
    * `+ save(member: Member): void` : 신규 가입 회원 또는 정보가 수정된 회원 데이터를 DB에 저장
* **Description**
    * `MemberService` 클래스는 Service의 역할을 한다. 컨트롤러와 리포지토리 사이에서 회원 데이터를 검증 및 조회하고, 피드백 점수를 기반으로 한 자가 학습 가중치 업데이트 연산을 처리하는 클래스이다.

### 5) WeatherService

* **Attributes**
    * `- weatherApiUrl: String` : 기상청 API와 통신하기 위한 외부 서버 URL 주소
    * `- apiKey: String` : 외부 API 통신을 위해 발급받은 인증 키
* **Methods**
    * `+ fetchCurrentTemperature(nx: Int, ny: Int): Double` : 변환된 사용자의 격자 좌표(nx, ny)를 외부 API에 전송하여 해당 지역의 실시간 현재 기온 데이터를 가져오기
* **Description**
    * `WeatherService` 클래스는 Service의 역할을 한다. 기상청 등 외부 API 서버와 통신하여 추천 알고리즘의 기준값으로 사용될 실시간 기상 데이터를 수신하는 클래스이다.

### 6) MemberRepository

* **Attributes**
    * (없음)
* **Methods**
    * `+ findById(id: Long): Member` : 데이터베이스 고유 식별자(ID)로 특정 회원 정보 조회
    * `+ findByLoginId(loginId: String): Member` : 사용자의 로그인 아이디로 회원 정보 조회
* **Description**
    * `MemberRepository` 인터페이스는 데이터 접근(Repository) 역할을 한다. 데이터베이스의 회원(Member) 테이블에 직접 접근하여 쿼리를 수행하고 데이터를 조회 및 관리하는 기능을 제공한다.

### 7) ClothesRepository

* **Attributes**
    * (없음)
* **Methods**
    * `+ findByMemberId(memberId: Long): List<Clothes>` : 특정 사용자의 고유 식별자(ID)로 등록된 모든 옷 목록 조회
    * `+ findByMemberIdAndTpo(memberId: Long, tpo: String): List<Clothes>` : 특정 사용자의 옷 중, 선택된 TPO 상황에 부합하는 옷들만 필터링하여 조회
    * `+ deleteById(id: Long): void` : 고유 식별자(ID)를 이용해 특정 옷 데이터를 DB에서 영구 삭제
* **Description**
    * `ClothesRepository` 인터페이스는 데이터 접근(Repository) 역할을 한다. 데이터베이스의 옷장(Clothes) 테이블에 접근하여 특정 회원의 의류 목록을 조건별(두께, TPO 등)로 검색하고 관리하는 기능을 제공한다.

### 8) Member

* **Attributes**
    * `- id: Long` : 회원 데이터의 DB 고유 식별자(Primary Key)
    * `- loginId: String` : 로그인 시 사용하는 아이디
    * `- password: String` : 로그인 시 사용하는 비밀번호
    * `- name: String` : 회원의 이름 또는 닉네임
    * `- constitutionWeight: Double` : 사용자의 피드백을 통해 동적으로 학습되는 개인별 열 민감도 가중치 (실수형)
* **Methods**
    * `+ updateConstitutionWeight(score: Double): void` : 계산된 새로운 가중치 값을 객체에 적용 및 업데이트
    * `+ getConstitutionWeight(): Double` : 현재 설정된 체질 가중치 값을 가져오기
* **Description**
    * `Member` 클래스는 Model(Entity)의 역할을 한다. 데이터베이스의 회원 테이블과 1:1로 매핑되며, 사용자의 기본 계정 정보와 추천 알고리즘의 핵심인 '체질 가중치' 데이터를 영속적으로 담고 있는 클래스이다.

### 9) Clothes

* **Attributes**
    * `- id: Long` : 옷 데이터의 DB 고유 식별자(Primary Key)
    * `- name: String` : 옷의 식별용 이름 (예: 검정 슬랙스)
    * `- category: String` : 상의, 하의, 아우터 등 옷의 분류 카테고리
    * `- thickness: String` : 옷의 두께감(계절감) 속성
    * `- tpo: String` : 옷을 입기에 적합한 외출 목적(TPO) 태그
    * `- member: Member` : 이 옷을 소유하고 있는 회원 객체 (N:1 양방향 매핑)
* **Methods**
    * `+ getThickness(): String` : 특정 옷의 두께 속성 가져오기
    * `+ getTpo(): String` : 특정 옷의 상황 태그 가져오기
* **Description**
    * `Clothes` 클래스는 Model(Entity)의 역할을 한다. 회원이 시스템(옷장)에 등록한 개별 의류 데이터를 담고 있으며, `Member` 클래스와 다대일(N:1) 관계로 매핑되어 데이터를 구성하는 클래스이다.

### 10) FeedbackRequest

* **Attributes**
    * `- score: Double` : 사용자가 선택한 5단계 체감 온도 피드백 점수 (-2.0 ~ +2.0)
* **Methods**
    * `+ getScore(): Double` : 객체에 담긴 피드백 점수 데이터 가져오기
* **Description**
    * `FeedbackRequest` 클래스는 클라이언트와 서버 간의 데이터 전송 객체(DTO) 역할을 한다. 메인 화면에서 발생한 피드백 점수 데이터를 포장하여 백엔드 컨트롤러로 안전하게 전달하는 전용 클래스이다.
 
<br>

### [Frontend Classes]
<img width="1514" height="1088" alt="image" src="https://github.com/user-attachments/assets/a6fd4821-7347-4c7c-a2ab-57b77abf3f5f" />

프론트엔드는 사용자와 직접 상호작용하는 UI 화면(View) 단위로 클래스를 설계하였다. 각 클래스는 사용자의 이벤트를 감지하고 백엔드 API를 호출하여 화면을 렌더링하는 역할을 수행한다.

### 1) IndexView (index.html)

* **Attributes**
    * `- loginIdInput: Element` : 사용자 아이디 입력 필드 (`#loginId`)
    * `- passwordInput: Element` : 사용자 비밀번호 입력 필드 (`#password`)
    * `- loginForm: Element` : 로그인 폼 컨테이너 (`#loginForm`)
* **Methods**
    * `+ submitLogin(Event e): void` : 폼 제출(submit) 이벤트를 감지하여 기본 동작을 막고(`preventDefault`), 백엔드 API(`/api/members/login`)로 사용자 인증 정보를 전송한다.
    * `+ handleLoginSuccess(data: JSON): void` : 로그인 성공 시 서버로부터 응답받은 데이터(닉네임, 체질 가중치 등)를 브라우저의 `localStorage`에 저장하고, 메인 화면(`main.html`)으로 사용자를 리다이렉트한다.
    * `+ handleLoginError(): void` : 통신 오류나 로그인 실패 시 사용자에게 알림(Alert)을 표시하여 예외를 처리한다.
* **Description**
    * `IndexView`는 시스템 진입 시 가장 먼저 렌더링되는 초기 로그인 화면이다. Fetch API를 이용한 비동기 통신으로 백엔드와 상호작용하며, 로그인 성공 시 발급되는 사용자 식별 정보와 세션 데이터를 클라이언트 브라우저 로컬 스토리지에 안전하게 보관하는 역할을 수행한다.

### 2) SignupView (signup.html)

* **Attributes**
    * `- nicknameInput: Element` : 사용자 닉네임 입력 필드 (`#nickname`)
    * `- loginIdInput: Element` : 사용자 아이디 입력 필드 (`#loginId`)
    * `- passwordInput: Element` : 사용자 비밀번호 입력 필드 (`#password`)
    * `- locationInput: Element` : 사용자 주소/위치 입력 필드 (`#location`)
    * `- joinForm: Element` : 회원가입 폼 컨테이너 (`#joinForm`)
* **Methods**
    * `+ searchLocation(): void` : 주소 검색 API(추후 연동 예정)를 호출하여 사용자의 위치 정보를 입력받는 이벤트 핸들러이다.
    * `+ submitSignup(Event e): void` : 폼 제출(submit) 이벤트를 감지하여 기본 동작을 막고, 입력된 필드 값들을 `memberData` 객체(JSON)로 포장하여 백엔드 API(`/api/members/signup`)로 POST 요청을 보낸다.
    * `+ handleSignupSuccess(message: String): void` : 회원가입 성공 응답을 받으면 서버의 환영 메시지를 띄우고, 자동 로그인을 위해 클라이언트 `localStorage`에 아이디(`loggedInUser`)를 저장한 뒤 체질 설정 화면(`constitution.html`)으로 이동시킨다.
    * `+ handleSignupError(error: Error): void` : 백엔드 처리 실패나 네트워크 통신 오류 발생 시 에러 메시지를 화면에 출력(Alert)하여 예외를 처리한다.
* **Description**
    * `SignupView`는 신규 사용자가 시스템 계정을 생성하기 위한 UI 컴포넌트이다. 폼 요소들로부터 데이터를 수집하여 JSON 형태로 서버에 전송하며, 회원가입 완료 후 추천 알고리즘에 필요한 초기 체질 설정을 유도하기 위해 관련된 화면으로 부드럽게 라우팅(Routing)하는 역할을 전담한다.

### 3) ConstitutionView (constitution.html)

* **Attributes**
    * `- constitutionForm: Element` : 체질 설정 폼 컨테이너 (`#constitutionForm`)
    * `- tempPrefRadios: NodeList` : 사용자의 열 민감도(추위/더위 타는 정도)를 5단계로 입력받는 라디오 버튼 그룹 (`input[name="temp-pref"]`)
* **Methods**
    * `+ submitConstitution(Event e): void` : 폼 제출(submit) 이벤트를 감지하여 기본 동작을 막고, 사용자가 선택한 라디오 버튼의 가중치 값(-2 ~ +2)을 추출한다.
    * `+ saveConstitutionLocally(value: String): void` : 추출한 체질 가중치 값을 클라이언트의 브라우저 `localStorage`(`user_constitution`)에 저장한다. 불필요한 서버 통신을 줄이고, 향후 메인 화면에서 추천 알고리즘 구동 시 이 상태값을 꺼내어 사용하기 위한 목적이다.
    * `+ navigateToMain(): void` : 설정 완료 알림(Alert)을 띄운 직후, 메인 홈 화면(`main.html`)으로 라우팅한다.
* **Description**
    * `ConstitutionView`는 회원가입 직후 사용자의 초기 열 민감도(체질 가중치)를 설정하는 UI 컴포넌트이다. 직관적인 5단계 라디오 버튼을 통해 데이터를 수집하며, 서버 API 호출 대신 클라이언트 로컬 스토리지를 활용한 상태 관리 기법을 적용하여 프론트엔드의 성능과 효율을 높였다.
 
### 4) MainView (main.html)

* **Attributes**
    * `- pendingFeedback: Boolean` : 사용자가 추천을 받은 후 아직 피드백을 남기지 않은 '대기 상태'인지를 추적하는 핵심 플래그 변수
    * `- currentTempDisplay: Element` : 화면에 렌더링되는 실시간 기온 텍스트 요소 (`#current-temp`)
    * `- recommendForm: Element` : TPO 선택 및 추천 요청 이벤트를 감지하는 폼 (`#recommendForm`)
    * `- recommendResultArea: Element` : 백엔드로부터 응답받은 추천 결과가 출력되는 영역 (`#recommend-result`)
    * `- feedbackModal: Element` : 피드백 입력을 위한 오버레이 모달 창 (`#feedbackModal`)
    * `- feedbackForm: Element` : 체감 온도 점수를 전송하는 폼 (`#feedbackForm`)
* **Methods**
    * `+ initializeView(): void` : 페이지 로드 시(`window.onload`) 실행된다. `localStorage`의 로그인 세션을 검증하여 미인증 접근을 차단하며, 백엔드 API(`/api/main/member/nickname`)를 비동기 호출해 사용자의 실제 닉네임을 모달 UI에 동적으로 바인딩한다.
    * `+ editTemperature(): void` : 날씨 API 장애나 오차를 대비하여, 사용자에게 `prompt` 창을 띄워 현재 기온을 수동으로 보정할 수 있게 하는 예외 처리 인터페이스이다.
    * `+ requestRecommendation(Event e): void` : 선택된 TPO와 기온을 바탕으로 백엔드(`/api/main/recommendations`)에 GET 요청을 보낸다. 추천이 성공적으로 렌더링되면 `pendingFeedback`을 `true`로 전환하여 피드백 제공을 유도한다.
    * `+ openFeedbackModal(): void` : `pendingFeedback` 상태값을 검증하여, 추천을 받지 않은 상태에서의 무의미한 피드백 시도를 사전에 차단하고 모달 창을 띄운다.
    * `+ submitFeedback(Event e): void` : 선택된 피드백 점수(-2~+2)와 당시의 기온(tempAtTime)을 JSON 포맷으로 패키징하여 백엔드(`/api/main/feedback`)로 POST 전송한다. 통신 성공 시 모달을 닫고 `pendingFeedback` 상태를 `false`로 해제한다.
    * `+ skipFeedback(): void` : 피드백 제공을 원치 않는 사용자를 위해 상태를 초기화하고 창을 닫는 UX 편의성 메서드이다.
* **Description**
    * `MainView`는 본 시스템의 핵심 비즈니스 흐름(추천 ➔ 피드백 ➔ 가중치 학습)을 클라이언트 단에서 제어하는 메인 컨트롤 타워이다. 단순한 화면 렌더링을 넘어 상태값(`pendingFeedback`)을 활용한 논리적인 뷰(View) 제어와 다수의 비동기 API 통신을 유기적으로 처리한다.

### 5) WardrobeView (wardrobe.html)

* **Attributes**
    * `- clothesContainer: Element` : 서버로부터 받아온 의류 목록 데이터가 동적으로 렌더링되는 부모 컨테이너 영역 (`#clothes-container`)
    * `- addClothesBtn: Element` : 새 옷 등록 화면으로 이동하기 위한 상단 버튼 (`.add-clothes-btn`)
* **Methods**
    * `+ initializeView(): void` : 페이지 로드 시(`window.onload`) 실행된다. 클라이언트 로컬 스토리지의 인증 세션을 검증하고, 문자열 전처리(정규식 및 공백 제거)를 통해 식별자를 정제한 후 데이터 조회를 위임한다.
    * `+ fetchClothes(loginId: String): void` : 백엔드 API(`/api/main/clothes`)로 GET 통신을 수행한다. 응답받은 JSON 배열 객체를 순회하며 `clothes-item` DOM 요소를 동적으로 생성 및 화면에 부착(Append)한다. 옷장이 비어있을 경우 빈 상태(Empty State) UI를 출력하여 사용자 경험을 보완한다.
    * `+ deleteCloth(clothesId: Long): void` : 각 의류 항목의 휴지통 버튼 클릭 시 호출된다. 자바스크립트의 `confirm` 다이얼로그를 통해 사용자의 의도를 재확인한 뒤, 백엔드 API(`/api/main/clothes/{clothesId}`)로 DELETE 요청을 보내 데이터를 영구 삭제하고 화면을 새로고침(`location.reload`)하여 상태를 동기화한다.
    * `+ navigateToAddClothes(): void` : 사용자가 새로운 옷을 등록하려 할 때 입력 폼 화면(`add_clothes.html`)으로 라우팅한다.
* **Description**
    * `WardrobeView`는 사용자의 디지털 옷장을 시각화하고 관리(조회/삭제)하는 전담 UI 컴포넌트이다. 백엔드 통신 결과를 바탕으로 클라이언트 단에서 DOM 트리를 실시간으로 조작하여 화면을 구성하며, 삭제 시 즉각적인 뷰 업데이트를 통해 서버 데이터와 프론트엔드 화면 간의 정합성을 유지한다.

### 6) AddClothesView (add_clothes.html)

* **Attributes**
    * `- addClothesForm: Element` : 의류 정보 입력을 총괄하는 폼 컨테이너 (`#addClothesForm`)
    * `- nameInput: Element` : 옷의 이름을 입력받는 텍스트 필드 (`#clothesName`)
    * `- categoryRadios: NodeList` : 상의, 하의, 아우터 등 카테고리를 선택하는 라디오 버튼 그룹 (`input[name="category"]`)
    * `- thicknessRadios: NodeList` : 옷의 두께감(얇음/보통/두꺼움)을 선택하는 라디오 버튼 그룹 (`input[name="thickness"]`)
    * `- tpoRadios: NodeList` : 옷의 착용 목적(운동/일상/격식)을 선택하는 라디오 버튼 그룹 (`input[name="tpo"]`)
* **Methods**
    * `+ validateSession(): Boolean` : 폼 전송 시도 시 클라이언트 로컬 스토리지의 사용자 식별자(`loggedInUser`) 유무를 검증하고, 정규식을 통해 특수문자(따옴표 등)를 정제하여 예외를 방어한다.
    * `+ formatClothesName(name: String, category: String): String` : 선택된 카테고리 값을 분석하여 상의(👕), 하의(👖), 아우터(🧥)에 맞는 이모지 아이콘을 원본 옷 이름 문자열 앞에 동적으로 결합(Mapping)하는 데이터 가공 메서드이다.
    * `+ submitNewClothes(Event e): void` : 폼의 기본 제출 동작(Submit)을 차단하고, 화면에서 수집 및 가공된 모든 의류 속성들을 JSON 객체로 직렬화(`JSON.stringify`)하여 백엔드 통신 API(`/api/main/clothes`)로 POST 요청을 보낸다.
    * `+ handleSuccess(): void` : 옷장 DB 저장 성공 시 성공 알림(Alert)을 띄우고, 최신 데이터 렌더링을 위해 즉시 옷장 조회 화면(`wardrobe.html`)으로 라우팅 처리한다.
* **Description**
    * `AddClothesView`는 사용자의 디지털 옷장에 새로운 의류 데이터를 등록하는 폼(Form) UI 컴포넌트이다. 직관적인 라디오 버튼 인터페이스를 통해 백엔드 DB의 열거형(Enum) 데이터와 일치하는 규격화된 입력을 유도하며, 프론트엔드 단에서 시각적 아이콘 결합 로직을 처리하여 데이터의 풍부함을 더하는 역할을 수행한다.

### 7) MyPageView (mypage.html)

* **Attributes**
    * `- nicknameDisplay: Element` : 서버에서 받아온 사용자의 실제 닉네임이 렌더링되는 텍스트 요소 (`#display-nickname`)
    * `- baseConstitutionDisplay: Element` : 초기 설정된 기본 체질 텍스트 요소 (`#display-base-constitution`)
    * `- currentConstitutionDisplay: Element` : 피드백을 통해 현재 학습 및 적용된 체질 텍스트 요소 (`#display-constitution`)
    * `- sliderDots: NodeList` : 체질 단계를 시각적으로 보여주는 슬라이더 UI의 점(Dot) 요소들 (`.slider-dot`)
* **Methods**
    * `+ initializeView(): void` : 페이지 로드 시(`window.onload`) 실행된다. 로컬 스토리지의 인증 식별자를 정제한 후 백엔드 API(`/api/main/member/nickname`)를 호출하여 화면에 실제 닉네임을 바인딩한다. 동시에 저장된 체질 가중치 데이터를 읽어와 UI를 초기화한다.
    * `+ renderConstitutionUI(baseConst: String, savedConst: String): void` : 숫자 형태(-2 ~ +2)의 체질 가중치 값을 읽기 쉬운 텍스트(예: "❄️ 매우 추위를 많이 탐")로 변환하고, 해당하는 슬라이더 점 요소에 `active` 클래스를 동적으로 부여하여 사용자의 현재 상태를 직관적으로 시각화한다.
    * `+ logout(): void` : 로그아웃 버튼 클릭 시 호출된다. `confirm` 다이얼로그로 사용자의 의도를 확인한 후, `localStorage.clear()`를 실행하여 클라이언트 브라우저에 저장된 모든 세션 및 상태 데이터를 안전하게 파기하고 로그인 화면(`index.html`)으로 라우팅한다.
    * `+ navigateToConstitutionReset(): void` : 체질 재설정 버튼 클릭 시, 가중치 초기화를 위해 체질 설정 화면(`constitution.html`)으로 이동시킨다.
* **Description**
    * `MyPageView`는 사용자의 프로필 정보를 확인하고 시스템 설정을 관리하는 마이페이지 UI 컴포넌트이다. 백엔드 통신을 통한 정보 동기화, 클라이언트 스토리지 데이터를 기반으로 한 시각적 UI 렌더링, 그리고 안전한 세션 만료(로그아웃) 처리 등 계정 및 상태 관리와 관련된 전반적인 프론트엔드 제어 역할을 수행한다.
 
<br>

## 3. Sequence diagram
### 3.1. Sign Up (회원가입)
<img width="1433" height="687" alt="image" src="https://github.com/user-attachments/assets/ff672a63-98a4-4413-81de-96d2ccdf09a1" />

**[다이어그램 설명]**
* **목적:** 신규 사용자가 시스템에 계정을 생성하는 과정을 나타낸다.
* **동작 흐름:**
  1. 사용자가 폼에 입력한 가입 정보(아이디, 비밀번호 등)를 `MainController`로 전송한다.
  2. 컨트롤러는 `MemberService`로 데이터를 전달하여 비즈니스 로직(검증 및 객체 생성)을 수행한다.
  3. 서비스는 `MemberRepository`를 호출하여 최종적으로 데이터베이스에 사용자 정보를 영구 저장한다.
  4. 저장이 완료되면 역순으로 결과를 반환하여 사용자에게 가입 성공 메시지를 출력한다.

---

### 3.2. Login (로그인)
<img width="1409" height="720" alt="image" src="https://github.com/user-attachments/assets/dd4f4104-9075-495b-896d-06d06a6d5fe9" />

**[다이어그램 설명]**
* **목적:** 기존 사용자가 시스템에 접근하기 위해 인증을 수행하는 과정을 나타낸다.
* **동작 흐름:**
  1. 사용자가 로그인 자격 증명(아이디, 비밀번호)을 `MainController`에 제출한다.
  2. `MemberService`가 `MemberRepository`를 통해 해당 아이디를 가진 사용자 엔티티를 조회한다.
  3. DB로부터 반환된 정보를 바탕으로 비밀번호 일치 여부를 검증한 후, 성공 시 사용자에게 인증 세션(로컬 스토리지 등)을 부여하고 메인 화면으로 전환한다.

---

### 3.3. Set Profile (프로필 및 체질 설정)
<img width="1450" height="845" alt="image" src="https://github.com/user-attachments/assets/16e859eb-3792-4cec-a169-d6588a98f9c1" />

**[다이어그램 설명]**
* **목적:** 맞춤형 알고리즘을 위해 사용자의 초기 열 민감도(추위/더위 타는 정도)를 설정하는 과정이다.
* **동작 흐름:**
  1. 사용자가 5단계 척도 중 하나를 선택하여 `MainController`로 전송한다.
  2. `MemberService`는 해당 사용자를 DB에서 조회한 후, 입력받은 값으로 사용자의 체질 가중치(Constitution Weight) 속성을 갱신한다.
  3. 변경된 상태를 `MemberRepository`를 통해 DB에 반영하고 설정 완료를 사용자에게 알린다.

---

### 3.4. Manage Wardrobe (옷장 관리: 등록/조회/삭제)
<img width="1060" height="989" alt="image" src="https://github.com/user-attachments/assets/39582a54-fbc1-49ef-a6a5-e70b1bd74d59" />

**[다이어그램 설명]**
* **목적:** 사용자가 자신의 디지털 옷장 내 의류 데이터를 종합적으로 관리(CRUD)하는 흐름을 나타낸다.
* **동작 흐름:**
  * 하나의 유스케이스 안에서 사용자의 선택에 따라 분기 처리(`alt` 블록)가 발생한다.
  * **Add:** 회원을 검증한 후 전달받은 의류 정보(카테고리, 두께, TPO 등)를 DB에 삽입한다.
  * **View:** 특정 회원 ID와 매핑된 전체 의류 목록(`List<Clothes>`)을 DB에서 조회하여 반환한다.
  * **Delete:** 특정 의류 식별자(ID)를 이용해 해당 레코드를 영구 삭제한다.
  * 각 동작이 완료되면 프론트엔드 단에서 DOM을 갱신하여 최신 옷장 상태를 사용자에게 보여준다.

---

### 3.5. Request Guide (옷차림 추천 요청)
<img width="1522" height="736" alt="image" src="https://github.com/user-attachments/assets/86fb69d2-f2cb-4f63-a865-7f19dacb8b0c" />

**[다이어그램 설명]**
* **목적:** 본 시스템의 핵심 비즈니스 로직으로, 외부 날씨 데이터와 사용자의 프로필, 옷장 데이터를 종합하여 최적의 옷차림을 추천한다.
* **동작 흐름:**
  1. `MainController`가 요청을 받으면, 가장 먼저 외부 공공데이터 포털(`Weather API`)을 호출하여 실시간 기온을 획득한다.
  2. `RecommendationService`에서 기온과 사용자의 체질 가중치를 연산하여 '목표 두께'를 산출한다.
  3. `ClothesRepository`를 통해 해당 두께와 TPO에 맞는 옷을 필터링한다.
  4. **(예외 처리)** 만약 일치하는 두께의 옷이 옷장에 없을 경우, `alt` 분기를 통해 두께 조건을 완화한 Plan B(대체 의류)를 재검색하여 추천 실패를 방지한다.

---

### 3.6. Provide Feedback (피드백 제공 및 가중치 업데이트)
<img width="1461" height="756" alt="image" src="https://github.com/user-attachments/assets/5523c14f-303b-4af8-8460-848875899ce3" />

**[다이어그램 설명]**
* **목적:** 추천받은 옷차림에 대한 사용자의 체감(더움/추움) 피드백을 수집하여 알고리즘의 정확도를 개선하는 기계학습 기초 흐름이다.
* **동작 흐름:**
  1. 사용자가 피드백 모달창에서 평가 점수(-2 ~ +2)를 선택하여 전송한다.
  2. `MemberService`는 기존 사용자의 체질 데이터에 새로운 피드백 점수를 연산하여 새로운 체질 가중치로 보정한다.
  3. `MemberRepository`를 통해 변경된 가중치 값을 DB에 덮어쓰기하여, 다음 추천 시 더욱 개인화된 결과를 제공할 수 있도록 시스템 상태를 진화시킨다.
 
<br>

## 4. State Machine Diagram

본 장에서는 WhatToWear 시스템의 클라이언트와 서버 측면에서 시스템이 가질 수 있는 주요 상태(State)와 상태 간의 전이(Transition)를 명세한다. 특히 각 상태 내부의 진입(`entry/`), 수행(`do/`) 액션과 상태를 전이시키는 `Event [Guard] / Action` 형식의 UML 표준 표기법을 적용하여 설계하였다.

### 4.1. Client System State Machine

### 4.1. Frontend State Machine
<img width="1270" height="830" alt="image" src="https://github.com/user-attachments/assets/16c626aa-69cb-44cd-a05e-2908bcf41f67" />

**[다이어그램 설명]**

* **목적:** 사용자의 웹 브라우저 접속부터 종료 시점까지 발생하는 프론트엔드의 화면 상태(View) 변화와 실제 DOM 이벤트 및 세션 검증에 따른 흐름을 명세한다.
* **주요 상태 그룹(Composite State) 구조:**
    * **Unauthenticated (비인증 상태):** 로그인 및 회원가입 처리가 완료되지 않은 상태로, 내부적으로 `IndexView`, `SignupView`, `ConstitutionView` 하위 상태를 포함한다.
    * **Authenticated (인증 상태):** 로그인 인증에 성공한 상태로 브라우저의 `localStorage` 세션을 유지하며, 내부적으로 `MainView`, `WardrobeView`, `MyPageView` 등의 하위 화면 상태를 제어한다.

* **상태 전이 흐름 상세:**
    1. **초기 진입 및 비인증 흐름 (Unauthenticated 영역)**
        * 사용자가 `웹 사이트 접속` 시 첫 진입점인 `IndexView` 상태가 된다.
        * `IndexView`에서 `회원가입 링크 클릭` 이벤트 발생 시 `SignupView` 상태로 전이되며, 가입 양식을 채워 `joinForm 제출 완료` 시 초기 열 민감도를 설정하는 `ConstitutionView` 상태로 이동한다.
        * `ConstitutionView` 상태에서 `constitutionForm 제출 완료` 이벤트가 발생하면 인증 영역인 `Authenticated` 상태로 전이된다.
        * 기존 회원의 경우 `IndexView`에서 `loginForm 전송 성공 (localStorage 저장)` 이벤트를 통해 즉시 `Authenticated` 영역의 `MainView`로 바로 진입할 수 있다.
    2. **로그인 인증 상태에서의 핵심 서비스 흐름 (Authenticated 영역)**
        * 인증 상태 진입 시 기본적으로 시스템의 중심이 되는 `MainView` 상태로 초기화된다.
        * **추천 요청:** `MainView`에서 `recommendForm 제출 (API 호출)` 시 서버의 응답을 기다리는 `Fetching_Recommendation` 상태(로딩)를 거쳐, 통신 완료 후 `옷차림 렌더링 (pendingFeedback = true)` 처리를 수행하며 다시 `MainView` 상태로 돌아온다.
        * **피드백 제공:** 추천 완료 후 변경된 상태 플래그에 의해 `openModal() 실행` 시 `FeedbackModal` 오버레이 뷰 상태로 전이되며, `feedbackForm 전송 완료 / 건너뛰기` 처리 시 팝업이 닫히며 원래의 `MainView` 상태로 복귀한다.
        * **옷장 관리:** 네비게이션 바를 통한 `Navbar(옷장) 탭 클릭` 시 `WardrobeView` 상태로 전환된다. 여기서 `새 옷 등록 버튼 클릭` 시 의류 추가 폼인 `AddClothesView` 상태가 되며, 입력을 마친 후 `addClothesForm 제출 (저장 완료)` 이벤트를 통해 다시 최신 옷장 목록을 보여주는 `WardrobeView` 상태로 회귀한다.
        * **마이페이지 및 로그아웃:** 사용자는 `MainView`나 `WardrobeView`에서 언제든 `Navbar(마이) 탭 클릭` 이벤트를 발생시켜 `MyPageView` 상태로 수평 전이할 수 있다. `MyPageView`에서 사용자가 `logout() 실행 (세션 초기화)` 버튼을 누르면 로컬 스토리지가 클리어되면서 다시 시스템 초기 상태인 `Unauthenticated` 영역의 `IndexView` 상태로 완전히 튕겨 나간다.
    3. **생명주기 종료 (End State)**
        * 사용자가 로그인을 하지 않은 상태(`Unauthenticated`) 혹은 로그인을 마친 상태(`Authenticated`)에 관계없이, 웹 브라우저 창을 닫는 `브라우저 종료` 이벤트 발생 시 즉시 최종 종료 상태(`[*]`)로 전이되며 클라이언트 시스템의 생명주기가 만료된다.
### 4.2. Backend State Machine

<br>

## 5. Implementation Requirements

본 시스템(WhatToWear)을 원활하게 구현하고 구동하기 위한 최소 하드웨어 및 소프트웨어 요구사항은 다음과 같다.

### 1) H/W platform requirements
* **CPU:** Intel Core i3 (또는 동급) 이상
* **RAM:** 4GB 이상
* **Storage:** 10GB 이상의 여유 공간
* **Client Device:** 인터넷 접속 및 웹 브라우저 구동이 가능한 스마트 기기 또는 PC

### 2) S/W platform requirements
* **Operating System:** Windows 10 이상 또는 macOS
* **Implementation Language:** Java (JDK 17 이상), HTML5 / JavaScript
* **Framework / DB:** Spring Boot 3.x, MySQL 8.0 (또는 로컬 테스트용 H2 DB)
* **Web Browser:** Google Chrome, Safari 등 최신 모던 브라우저

<br>

## 6. Glossary

본 장에서는 WhatToWear 시스템의 소프트웨어 설계 및 다이어그램 명세(Class, Sequence, State Machine Diagram 등) 과정에서 사용된 주요 객체지향 및 아키텍처 관련 기술 용어를 정의한다.

* **Attribute (속성):**
  클래스(Class)가 가지는 데이터나 상태 값을 저장하는 변수이다. 본 설계에서는 `- loginId: String`, `- constitutionWeight: Double` 등과 같이 가시성(Visibility) 기호와 타입(Type)을 함께 명세하였다.

* **Method (메서드):**
  클래스 내부에 정의되어 특정 동작이나 비즈니스 로직을 수행하는 함수이다. 본 시스템에서는 `+ getRecommendation()`, `+ fetchCurrentTemperature()` 등 객체 간 상호작용을 위해 호출되는 핵심 행위들을 명세하였다.

* **Use Case (유스케이스):**
  시스템 외부의 액터(사용자)가 시스템을 통해 달성하고자 하는 목적이나 기능적 요구사항의 단위를 의미한다. 본 보고서의 초기 개념화(Conceptualization) 단계에서 도출되어 이후 시퀀스 다이어그램 작성의 기준이 되었다.

* **Controller / Service / Repository:**
  본 시스템의 백엔드 아키텍처를 구성하는 3계층(3-Tier) 패턴의 핵심 요소이다. 
  - **Controller:** 사용자의 요청(HTTP Request)을 수신하고 응답을 반환하는 역할.
  - **Service:** 데이터 가공, 알고리즘 연산 등 실질적인 비즈니스 로직을 처리하는 역할.
  - **Repository:** 데이터베이스(DB)에 접근하여 데이터를 저장, 조회, 삭제하는 데이터 접근 역할.

* **Entity / Model (엔티티 / 모델):**
  데이터베이스의 테이블과 1:1로 매핑되는 도메인 객체이다. 본 시스템에서는 회원 정보를 담는 `Member` 클래스와 옷 정보를 담는 `Clothes` 클래스가 이에 해당하며 영속성(Persistence) 데이터를 관리한다.

* **State & Transition (상태 및 전이):**
  상태 머신 다이어그램에서 객체나 화면이 특정 시점에 머물고 있는 상황을 **상태(State)**라 하며, 특정 이벤트(Event)가 발생하여 다른 상태로 넘어가는 과정을 **전이(Transition)**라 한다.

* **Guard (조건/가드):**
  상태가 다음 상태로 전이(Transition)되기 위해 반드시 만족해야 하는 논리적 제약 조건이다. 다이어그램 상에서 대괄호 `[ ]`로 표기되며, 본 설계에서는 `[인증 성공]` 또는 `[pendingFeedback == true]` 등으로 활용되었다.

* **Action (액션):**
  특정 상태에 진입(`entry/`), 머무는 동안(`do/`), 혹은 전이 화살표(`/`) 위에서 시스템이 수행하는 구체적인 동작이나 함수 호출(예: `fetchClothes() 실행`)을 의미한다.

## 7. References

[1] 영남대학교(Yeungnam University) 컴퓨터공학과, "OSS Design: Lecture #8 Behavior modeling I" 강의 자료. (시퀀스 다이어그램 설계 및 표기법 참고)

[2] 영남대학교(Yeungnam University) 컴퓨터공학과, "OSS Design: Lecture #9 Behavior modeling II" 강의 자료. (상태 머신 다이어그램 전이 조건 및 액션 설계 참고)

[3] 기상청, "단기예보(동네예보) 조회서비스 오픈API 활용가이드", 공공데이터포털(data.go.kr).
